<h1>Experiment "{{ experiment.name }}" has started!</h1>

<p>It will end {{ experiment.period.end | time_diff }}. Here is your outcome(s) overview:</p>

<table>
    <thead>
    <tr>
        <th>Expected outcome</th>
        <th>Initial value</th>
        <th>Expected value {{ experiment.period.end | time_diff }}</th>
    </tr>
    </thead>
    <tbody>
    {% for expectedOutcome in experiment.expectedOutcomes %}
        <tr>
            <td>{{ expectedOutcome.name }}</td>
            <td>{{ expectedOutcome.currentValue }}</td>
            <td>{{ expectedOutcome.expectedValue }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<p>
    <a href="{{ url('experiment', {'id': experiment.uuid}) }}" class="btn">Open experiment</a>
</p>

<p>See you there, <br/>Sam.</p>
