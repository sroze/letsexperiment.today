{% extends "base.html.twig" %}
{% block contents %}
<article class="home-hero row">
    <div class="promo col-md-6">
        <h2>Design, measure and track your experiments.</h2>
        <p>Building a product or trying to achieve a personal goal, we experiment every single day. Let's track these!</p>
    </div>

    <div class="create col-md-6">
        <form method="post" action="{{ path('create_experiment') }}">
            <div class="form-group">
                <label for="newExperiment">What's your experiment?</label>
                <input type="text" name="name" class="form-control" id="newExperiment" placeholder="Example: Use a tool to track my experiments..." required>
            </div>
            <button class="btn">Get started</button>
        </form>
    </div>
</article>
{% if pendingExperiments | length > 0 %}
    <h3>Your pending experiments</h3>
    <div class="experiments d-flex flex-wrap">
    {% for experiment in pendingExperiments %}
        {% include "homepage/partials/experiment_card.html.twig" with {'experiment': experiment} only %}
    {% endfor %}
    </div>
{% endif %}

{% if ongoingExperiments | length > 0 %}
    <h3>Your on-going experiments</h3>
    <div class="experiments d-flex flex-wrap">
        {% for experiment in ongoingExperiments %}
            {% include "homepage/partials/experiment_card.html.twig" with {'experiment': experiment} only %}
        {% endfor %}
    </div>
{% endif %}


{% if endedExperiments | length > 0 %}
    <h3>Your ended experiments</h3>
    <div class="experiments d-flex flex-wrap">
        {% for experiment in endedExperiments %}
            {% include "homepage/partials/experiment_card.html.twig" with {'experiment': experiment} only %}
        {% endfor %}
    </div>
{% endif %}
<hr/>
<article class="row features">
    <div class="col-md-4">
        <h4><i class="fa fa-key"></i> <span class="feature-name">Measurable results</span></h4>
        <p>When creating an experiment, you set a number of measurable <em>expected</em> outcomes. These will help you to conclude whether the experiment is successful or not.</p>
        <img src="{{ asset('images/summary.png') }}" />
    </div>
    <div class="col-md-4">
        <h4><i class="far fa-clock"></i> <span class="feature-name">Track them over-time</span></h4>
        <p>Every week, we will send you an overview of your on-going experiments and invite you to <em>check-in</em> to update the measurable expected outcomes.</p>
        <img src="{{ asset('images/over-time-2nd.png') }}" style="width: 100%;" />
    </div>
    <div class="col-md-4">
        <h4><i class="fa fa-users"></i> <span class="feature-name">Collaborate</span></h4>
        <p>Invite as many collaborators as you'd like to collaborate on your experiments. They will also be notified when something happens to the experimentation.</p>
        <img src="{{ asset('images/collaborators.png') }}" style="width: 100%;" />
    </div>
</article>
{% endblock %}
