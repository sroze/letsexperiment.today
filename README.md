# LetsExperiment.today

## Development

1. Install dependencies
   ```
   composer install && yarn install
   ```
   
2. Run the two following commands in parallel:
   ```
   bin/console server:run
   ```
   
   ```
   yarn run encore dev --watch
   ```
